= title t('tt.index', resource_name: Post.model_name.human)

- content_for :toolbar_left do
  = link_to(new_admins_post_path, class: 'btn', rel: 'tooltip', title: t('tooltips.new')) do
    %i.icon-plus


%table.table.table-striped.table-bordered.table-condensed
  %tr
    %th Heading
    %th Description
    %th{:width => "120px"}
  - @posts.each do |post|
    %tr
      %td= post.heading
      %td= post.description.to_s.html_safe
      %td
        = link_to(admins_post_path(post), class: 'btn', rel: 'tooltip', title: t('tooltips.show')) do
          %i.icon-eye-open
        = link_to(edit_admins_post_path(post) , class: 'btn', rel: 'tooltip', title: t('tooltips.edit')) do
          %i.icon-pencil
        = link_to(admins_post_path(post), class: 'btn', confirm: t('confirm'), method: :delete, rel: 'tooltip', title: t('tooltips.delete')) do
          %i.icon-trash